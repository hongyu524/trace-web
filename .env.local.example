# OpenAI API Key for AI memory planning
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI Model (defaults to gpt-5-mini if not specified)
OPENAI_MODEL=gpt-5-mini

# Server configuration
PORT=3001
HOST=127.0.0.1  # Use HOST=0.0.0.0 for LAN access (iOS testing)

# Frontend API base URL (optional, defaults to http://localhost:3001)
VITE_API_BASE_URL=http://localhost:3001
